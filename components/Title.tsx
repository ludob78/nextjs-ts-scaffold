import React, { ReactElement } from 'react'

interface Props {
    
}

function Title({}: Props): ReactElement {
    return (
        <h1>
            Best moments
        </h1>
    )
}

export default Title
